# Build outputs (regenerated by make)
build/
*.o
*.d
*.lst
*.map
*.elf
*.hex
*.bin
*.axf

# STM32Cube-generated HAL/CMSIS drivers -- fully regenerable from the .ioc via
# CubeMX ("Generate Code"). We only track our own Core/ sources + the .ioc +
# Makefile + linker/startup. Keeps the repo to hand-written/edited files only.
Drivers/

# Keil MDK-ARM IDE project + build artifacts (we build with the Makefile)
MDK-ARM/

# CubeMX internal state
.mxproject

# Editor / OS
*.swp
*~
.DS_Store
